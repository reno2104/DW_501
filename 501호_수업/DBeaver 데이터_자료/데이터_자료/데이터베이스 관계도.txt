데이터베이스 모델링

테이블들은 자식과 부모관계를 가진다.

관계 종류
1. 1:1 관계(One to One)
2. 1:N 관계(One to Many)
3. N:M 관계(Many to Many) -> 테이블 3개 이상

***모델링 Tip
1. 자주 조회하는 컬럼은 맨위에 작성하자.
	(조건을 찾기 위해 아래로 조회하는 특성)
2. 테이블에 컬럼은 많을 수록 좋지 않다.



